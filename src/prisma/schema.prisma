generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

model antispam {
  guild        String   @id
  setting      Boolean?
  messagecount Int?
  interval     Int?
}
